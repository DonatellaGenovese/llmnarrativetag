#!/bin/bash
# Copy to env.sh and fill in your local dataset paths (get via get_datasets.py).
# env.sh is machine-specific and gitignored — do not commit it.

export DATADIR_JetClass=/path/to/datasets/JetClass
export DATADIR_TopLandscape=/path/to/datasets/TopLandscape
export DATADIR_QuarkGluon=/path/to/datasets/QuarkGluon
